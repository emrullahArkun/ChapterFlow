spring.application.name=readflow
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}

# JWT Configuration
app.jwt.secret=${APP_JWT_SECRET}
app.jwt.ttl-seconds=3600

# Auth Configuration
app.auth.require-verification=false

# Open Library API
app.openlibrary.user-agent=ReadFlow (readflow@example.com)
